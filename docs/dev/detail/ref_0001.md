# ref_0001 - DB 어댑터 리팩토링 및 쿼리 외부화 반영

## Intent

- `feat/db-adapter-refactor` 브랜치의 개선된 구조(쿼리 외부화, 미배치 엔티티 처리)를 현재 브랜치의 스키마에 맞춰 반영하고 프롬프트를 고도화하여 데이터 일관성 및 관리 효율성 증대.

## Plan

- Step 1: `QueryLoader` 클래스 및 SQL/Cypher 외부 파일 구조 도입.
- Step 2: `adapter.py` 내의 하드코딩된 쿼리를 외부 파일로 이관.
- Step 3: 시퀀스F에 포함되지 않은 엔티티(NPC, Enemy, Item)를 저장하는 `create_unplaced_*` 로직 구현.
- Step 4: `feat/db-adapter-refactor` 브랜치의 프롬프트와 현재 프롬프트를 비교하여 더 상세하고 풍부한 내용으로 병합.
- Step 5: 변경된 ID 체계 및 저장 로직에 맞춰 기존 테스트 코드 업데이트.

## Safety net

- Tests to rely on: tests/test_infra_db.py, tests/test_logic_id_consistency.py, tests/test_scenario_workflow.py
- Command:
  - uv run pytest tests

## Done Criteria

- [ ] behavior preserved
- [ ] unit tests passing
- [ ] debt reduced: 하드코딩된 쿼리 제거 및 에셋 유실 없는 저장 구조 확보
